# Heap-Allocated-Contact-Database-Directory-Manager
A C-based contact database application that demonstrates structures, dynamic heap memory allocation, pointers, searching, custom sorting, and file handling using malloc(), realloc(), and free(). The project manages contact records dynamically and stores data in flat text files.
